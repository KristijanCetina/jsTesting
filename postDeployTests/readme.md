# Post relese test suit

## Setup

```bash
npm install && npx playwright install
```

## runing test

run

```bash
npm run test
```

OR

```bash
npx playwright test tests/postDeployTest.spec.ts --workers 1
```

Where parametar `--workers` is optional
